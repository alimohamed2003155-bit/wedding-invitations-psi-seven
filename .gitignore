node_modules/
.env
# ملفات بيئة إضافية (زي .env.production.local اللي فيه رابط قاعدة
# الإنتاج) — دي أسرار، عمرها ما تترفع. و.env.example بيفضل مرفوع
# لأنه قالب فاضي.
.env.*.local
!.env.example
.vercel
*.log
client/dist/

# الفيديو الأصلي بتاع العرض (52 ميجا) — بيفضل على جهازك بس.
# اللي بيترفع فعلًا هو النسخ المضغوطة في client/public/video/
# (2.4 ميجا للكل). رفع الأصل كان هيكبّر الريبو ووقت كل نشر من غير
# أي فايدة — المتصفح مش بيشوفه أصلًا.
client/src/vedio/
